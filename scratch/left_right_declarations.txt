
--- Declaration 1 ---

  font-family: var(--font-display);
  font-size: clamp(.9rem, 1.2vw, 1.05rem);
  font-weight: 300;
  font-style: italic;
  line-height: 1.8;
  color: var(--ink-muted);
  margin: 0;
  max-width: 42ch;
}

body /* ── LEFT — static, body section scrolls over fixed bg image ── */
.av16-left{
  position: static;
  top: auto;
  overflow: visible;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-self: start;
  padding-left: 0;
  margin-left: 0;
}

--- Declaration 2 ---

  font-size: clamp(0.95rem, 1.25vw, 1.25rem);
  letter-spacing: 0.26em;
}


body /* ── RIGHT — expanded to stretch so bottom line fits on one line ── */
.av16-right{
  display: flex;
  flex-direction: column;
  gap: 0;
  padding-top: 0;
  align-self: stretch;
  min-width: 0;
  max-width: 100%;
  margin-left: 0;
  padding-right: 0;
  padding-left: 2rem;
}

--- Declaration 3 ---

    grid-template-columns: 1fr;
    gap: 2rem;
    min-height: auto;
    align-items: start;
  }
  .av16-left {
    position: static;
    justify-content: flex-start;
    align-self: auto;
  }

--- Declaration 4 ---

  .av16-right { align-self: auto; }

--- Declaration 5 ---

  body .av16-left {
    transform: translate(2.5rem, 3.5rem);
  }

--- Declaration 6 ---

  display: grid;
  grid-template-columns: minmax(320px, 520px) 1fr;
  gap: clamp(3rem, 6vw, 7rem);
  align-items: stretch;
  min-height: 480px;
}
body /* Both columns fill their cell, body static — bg scrolls via background-attachment:fixed */
.av16-left,
body .av16-right{
  width: 100%;
  min-width: 0;
  max-width: none;
  position: static;
  top: auto;
  align-self: auto;
}

--- Declaration 7 ---

  border-top: none;
}

body /* ── Consolidated About Tagline & Alignment Overrides ── */
.av16-left{
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-self: center;
  position: static;
  top: auto;
  gap: 0;
}

--- Declaration 8 ---

  padding-top: 0;
  margin-top: 1.6rem;       /* just below KOMPASS */
  align-self: auto;
}
body .av16-right{
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-self: stretch;
  position: static;
}

--- Declaration 9 ---

  .av16-left  { justify-content: flex-start; align-items: center; }

--- Declaration 10 ---
 margin-top: 1.2rem; text-align: center; align-self: auto; }
  .av16-right { justify-content: flex-start; align-self: auto; }

--- Declaration 11 ---

    grid-template-columns: 1fr !important;
    gap: 2rem !important;
    min-height: auto !important;
  }
  body .av16-left,
  .av16-left {
    justify-content: center;
    align-items: center;
    text-align: center;
  }

--- Declaration 12 ---

  body .av16-right,
  .av16-right {
    width: 100% !important;
    max-width: 100% !important;
    padding-left: 0;
  }
